# Datos locales (fuera de git)

- Copia `.env.ejemplo` → `.env` (solo en tu máquina).
- Copia `usuarios.ejemplo.json` → `usuarios.json`.
- Coloca parquets/aggs pesados en `aggs/` de forma local (no se versionan).
- Los zips `*_App_y_Aggs` y catálogos grandes deben quedarse fuera del repositorio.
